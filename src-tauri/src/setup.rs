pub fn setup_app(app: &mut tauri::App) -> Result<(), Box<dyn std::error::Error>> {
    // TODO : Have the files in "examples" be copied to exe directory
    // then we can autoload from "examples/..."
    
        
    Ok(())
}

